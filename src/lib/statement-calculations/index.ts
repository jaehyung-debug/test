import{Prisma}from"@prisma/client";import{calculateQuotationItemValues}from"@/lib/calculations";export type StatementItemInput={name:string;specification?:string|null;unit:string;quantity:string;unitPrice:string;note?:string|null};const safe=(n:bigint)=>{if(n>BigInt(Number.MAX_SAFE_INTEGER))throw new RangeError("금액이 허용 범위를 초과했습니다.");return Number(n)};
export function calculateStatementPersistence(items:StatementItemInput[]){let subtotal=0n,vatAmount=0n,totalAmount=0n;const calculated=items.map((item,index)=>{const value=calculateQuotationItemValues(item.quantity,Number(item.unitPrice),0,"TAXABLE");subtotal+=value.supplyAmount;vatAmount+=value.vatAmount;totalAmount+=value.totalAmount;return{sortOrder:index+1,name:item.name,specification:item.specification||null,unit:item.unit,quantity:new Prisma.Decimal(item.quantity),unitPrice:safe(BigInt(item.unitPrice)),supplyAmount:safe(value.supplyAmount),vatAmount:safe(value.vatAmount),totalAmount:safe(value.totalAmount),note:item.note||null}});return{calculated,totals:{subtotal:safe(subtotal),vatAmount:safe(vatAmount),totalAmount:safe(totalAmount)}}}
