import{describe,expect,it}from"vitest";import{canDeleteSchedule,canEditCalendar,expandScheduleOccurrences,reminderAt,validateScheduleTimes}from"@/features/schedules/calculations";const base={id:"e",title:"일정",startAt:new Date("2026-08-01T09:00:00Z"),endAt:new Date("2026-08-01T10:00:00Z"),repeatInterval:1,repeatUntil:new Date("2026-10-31")};describe("일정 계산과 권한",()=>{it("종료와 반복 종료일을 검증한다",()=>{expect(()=>validateScheduleTimes({startAt:base.startAt,endAt:new Date("2026-07-31")})).toThrow();expect(()=>validateScheduleTimes({startAt:base.startAt,endAt:base.endAt,repeatUntil:new Date("2026-07-01")})).toThrow()});it("매일·매주·매월 반복을 종료일까지 가상 생성한다",()=>{const rangeStart=new Date("2026-08-01"),rangeEnd=new Date("2026-08-31T23:59:59Z");expect(expandScheduleOccurrences({...base,repeatType:"DAILY"},rangeStart,new Date("2026-08-03T23:00:00Z"))).toHaveLength(3);expect(expandScheduleOccurrences({...base,repeatType:"WEEKLY"},rangeStart,rangeEnd)).toHaveLength(5);expect(expandScheduleOccurrences({...base,repeatType:"MONTHLY"},rangeStart,new Date("2026-10-31"))).toHaveLength(3)});it("개인·공유 권한과 삭제 권한을 구분한다",()=>{expect(canEditCalendar(undefined,true)).toBe(true);expect(canEditCalendar("EDITOR",false)).toBe(true);expect(canEditCalendar("VIEWER",false)).toBe(false);expect(canDeleteSchedule({isCreator:false,isAdmin:false,isOwner:false})).toBe(false);expect(canDeleteSchedule({isCreator:true,isAdmin:false,isOwner:false})).toBe(true)});it("알림 예정 시각을 계산한다",()=>{expect(reminderAt(base.startAt,30)).toEqual(new Date("2026-08-01T08:30:00Z"));expect(reminderAt(base.startAt,null)).toBeNull()})});
