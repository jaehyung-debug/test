import{describe,expect,it}from"vitest";import{buildOrderStages,calculateOrderProgress,canReopenStage,stageDelayDays}from"@/features/orders/calculations";describe("오더 흐름 계산",()=>{it("템플릿 순서와 기본 소요일로 계획일을 만든다",()=>{const rows=buildOrderStages([{stageCode:"B",stageName:"생산",sortOrder:2,defaultDurationDays:2,isRequired:true},{stageCode:"A",stageName:"구매",sortOrder:1,defaultDurationDays:1,isRequired:true}],new Date("2026-08-01T00:00:00Z"));expect(rows.map(x=>x.stageCode)).toEqual(["A","B"]);expect(rows[0]).toMatchObject({status:"READY",plannedEndAt:new Date("2026-08-02T00:00:00Z")});expect(rows[1].plannedEndAt).toEqual(new Date("2026-08-04T00:00:00Z"))});it("완료·건너뜀 필수 단계 진행률을 계산한다",()=>{expect(calculateOrderProgress([{isRequired:true,status:"COMPLETED"},{isRequired:true,status:"SKIPPED"},{isRequired:true,status:"PENDING"},{isRequired:false,status:"PENDING"}])).toBeCloseTo(66.666)});it("지연일과 복원 권한을 계산한다",()=>{expect(stageDelayDays(new Date("2026-08-01"),null,new Date("2026-08-04"))).toBe(3);expect(canReopenStage("ADMIN")).toBe(true);expect(canReopenStage("MANAGER")).toBe(true);expect(canReopenStage("STAFF")).toBe(false)})});
