import { describe, expect, it } from "vitest";
import { calculateExpenseAmounts, calculateProjectCostSummary } from "@/features/project-costs/calculations";
describe("프로젝트 비용 계산",()=>{it("공급가액 기준 과세·면세와 수량을 계산한다",()=>{expect(calculateExpenseAmounts({quantity:2,unitPrice:1000,amountInputType:"SUPPLY",vatRate:10})).toEqual({supplyAmount:2000,vatAmount:200,totalAmount:2200});expect(calculateExpenseAmounts({quantity:1.5,unitPrice:1000,amountInputType:"SUPPLY",vatRate:0})).toEqual({supplyAmount:1500,vatAmount:0,totalAmount:1500})});it("부가세 포함 금액을 원 단위로 역산한다",()=>{expect(calculateExpenseAmounts({quantity:1,unitPrice:1001,amountInputType:"TOTAL",vatRate:10})).toEqual({supplyAmount:910,vatAmount:91,totalAmount:1001})});it("상태별 원가·이익·초과를 집계한다",()=>{const s=calculateProjectCostSummary(10000,5000,[{status:"APPROVED",supplyAmount:3000,vatAmount:300,totalAmount:3300},{status:"PAID",supplyAmount:2500,vatAmount:250,totalAmount:2750},{status:"DRAFT",supplyAmount:1000,vatAmount:100,totalAmount:1100},{status:"REJECTED",supplyAmount:9999,vatAmount:999,totalAmount:10998}]);expect(s).toMatchObject({actualCost:5500,plannedCost:1000,paidAmount:2750,vatAmount:650,remainingBudget:-500,expectedTotalCost:6500,expectedProfit:5000,currentExpectedProfit:3500,isOverBudget:true});expect(s.budgetUsageRate).toBe(110)})});
