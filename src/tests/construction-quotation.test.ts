import{describe,expect,it}from"vitest";import{calculateConstructionQuotationTotals,calculateQuotationItemAmount,groupQuotationItemsByCostCategory}from"@/lib/construction-quotation";import{parseQuotationPaste}from"@/lib/quotation-paste";
describe("공사용 견적 계산",()=>{it("비용구분과 자동 비율을 원 단위로 계산한다",()=>{const items=[{costCategory:"MATERIAL" as const,quantity:"1",unitPrice:"10000000",discountRate:"0"},{costCategory:"LABOR" as const,quantity:"1",unitPrice:"5000000",discountRate:"0"},{costCategory:"EXPENSE" as const,quantity:"1",unitPrice:"1000000",discountRate:"0"},{costCategory:"TRAVEL" as const,quantity:"1",unitPrice:"500000",discountRate:"0"}];const t=calculateConstructionQuotationTotals(items,{toolLossRate:3,generalAdminRate:5,safetyHealthRate:2.93});expect(t).toMatchObject({toolLossAmount:150000n,generalAdminAmount:750000n,safetyHealthAmount:439500n,taxableAmount:17839500n,vatAmount:1783950n,totalAmount:19623450n});expect(groupQuotationItemsByCostCategory(items).LABOR).toHaveLength(1)});it("할인과 소수 비율을 반올림한다",()=>expect(calculateQuotationItemAmount({quantity:"3",unitPrice:"333",discountRate:"2.5"}).supplyAmount).toBe(974n));it("붙여넣기 비용구분과 자동 항목 오류를 처리한다",()=>{expect(parseQuotationPaste("노무비\t작업\t\tMD\t1\t1,000\t0\t")[0]).toMatchObject({costCategory:"LABOR",unitPrice:"1000",errors:[]});expect(parseQuotationPaste("공구손료\t자동\t\t식\t1\t1\t0\t")[0].errors[0]).toContain("자동 계산")})});
