import{describe,expect,it}from"vitest";import{calculateDeliveryDelay,calculatePurchaseLine,calculatePurchaseTotals,calculateReceiptProgress,canExecuteDisbursement,findLowestQuotes,purchaseOrderStatusFromReceipts}from"@/features/procurement/calculations";
describe("구매/발주 계산",()=>{it("품목과 VAT를 원 단위로 재계산한다",()=>{expect(calculatePurchaseLine(1.5,1001)).toEqual({supplyAmount:1502,vatAmount:150,totalAmount:1652});expect(calculatePurchaseTotals([{quantity:2,unitPrice:1000},{quantity:1,unitPrice:500}],100)).toMatchObject({subtotal:2500,vatAmount:250,totalAmount:2650})});it("부분·전량·검수 입고 상태를 계산한다",()=>{expect(purchaseOrderStatusFromReceipts(10,0,0)).toBe("SENT");expect(purchaseOrderStatusFromReceipts(10,5,5)).toBe("PARTIALLY_RECEIVED");expect(purchaseOrderStatusFromReceipts(10,10,8)).toBe("RECEIVED");expect(purchaseOrderStatusFromReceipts(10,10,10)).toBe("INSPECTED");expect(calculateReceiptProgress(10,12,9)).toMatchObject({remaining:0,overReceived:2,receiptRate:120,inspectionRate:90})});it("납기 지연일을 계산한다",()=>expect(calculateDeliveryDelay(new Date("2026-08-01"),false,new Date("2026-08-03"))).toBe(2));it("집행 조건을 차단하고 충족 시 허용한다",()=>{expect(canExecuteDisbursement({trigger:"AFTER_FULL_RECEIPT",orderApproved:true,ordered:10,received:9,accepted:9}).allowed).toBe(false);expect(canExecuteDisbursement({trigger:"AFTER_INSPECTION",orderApproved:true,ordered:10,received:10,accepted:10}).allowed).toBe(true)});it("품목별 공동 최저가를 찾는다",()=>{const x=findLowestQuotes([{rfqItemId:"a",unitPrice:10,supplierQuotationId:"s1"},{rfqItemId:"a",unitPrice:10,supplierQuotationId:"s2"},{rfqItemId:"a",unitPrice:20,supplierQuotationId:"s3"}]);expect(x.get("a")).toEqual(["s1","s2"])});});
