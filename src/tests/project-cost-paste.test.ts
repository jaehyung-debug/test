import { describe, expect, it } from "vitest"; import { parseProjectCostPaste } from "@/lib/project-cost-paste";
const context={categories:[{id:"cat",name:"재료비"}],suppliers:[{id:"supplier",companyName:"LS일렉트릭"}]};
describe("프로젝트 비용 붙여넣기",()=>{it("헤더와 탭, 쉼표 금액, 한글 부가세, 빈 수량을 처리한다",()=>{const rows=parseProjectCostPaste("비용일자\t비용구분\t지급처\t비용명\t수량\t단가\t부가세\t지급방법\t증빙\t상태\t지급자\t메모\n2026-08-01\t재료비\tLS일렉트릭\tPLC\t\t3,500,000\t과세\t계좌이체\t세금계산서\t지급 완료\t관리자\t자재",context);expect(rows).toHaveLength(1);expect(rows[0]).toMatchObject({quantity:"1",unitPrice:"3500000",vatRate:10,supplierId:"supplier",status:"PAID",errors:[]})});it("잘못된 날짜와 없는 비용구분을 행 오류로 반환한다",()=>{const [row]=parseProjectCostPaste("2026-02-30\t없는구분\t\t비용\t1\t100\t없음\t현금\t증빙 없음\t예정\t\t",context);expect(row.errors.join(" ")).toContain("비용일자");expect(row.errors.join(" ")).toContain("존재하지 않는 비용구분")})});
