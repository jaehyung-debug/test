import Link from "next/link";import type { Product } from "@prisma/client";import { saveProduct } from "@/features/mvp/actions";import { input,label } from "@/components/common/page";import { SubmitButton } from "./submit-button";
export function ProductForm({product}:{product?:Product}){return <form action={saveProduct} className="mt-6 rounded-xl border bg-white p-6"><input type="hidden" name="id" value={product?.id}/><div className="grid grid-cols-2 gap-5"><label className={label}>품목코드 *<input className={input} required name="productCode" defaultValue={product?.productCode}/></label><label className={label}>품명 *<input className={input} required name="name" defaultValue={product?.name}/></label><label className={label}>규격<input className={input} name="specification" defaultValue={product?.specification??""}/></label><label className={label}>단위 *<input className={input} required name="unit" defaultValue={product?.unit??"개"}/></label><label className={label}>기본단가 *<input className={input} required min="0" step="1" type="number" name="defaultPrice" defaultValue={product?.defaultPrice.toString()??0}/></label><label className={label}>과세구분<select className={input} name="taxType" defaultValue={product?.taxType??"TAXABLE"}><option value="TAXABLE">과세</option><option value="ZERO_RATED">영세율</option><option value="TAX_EXEMPT">면세</option></select></label><label className={label}>카테고리<input className={input} name="category" defaultValue={product?.category??""}/></label></div><div className="mt-6 flex justify-end gap-3"><Link href="/products" className="rounded-lg border px-5 py-2.5 text-sm">취소</Link><SubmitButton/></div></form>}
