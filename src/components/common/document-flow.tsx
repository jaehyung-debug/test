import type{Route}from"next";import Link from "next/link";
export type DocumentFlowNode={type:string;id:string;number:string;label:string;href:string};
export function DocumentFlow({nodes,currentId}:{nodes:DocumentFlowNode[];currentId?:string}){if(!nodes.length)return null;return <div className="mb-5 overflow-x-auto rounded-xl border bg-slate-50 p-4"><p className="mb-2 text-xs font-semibold text-slate-500">문서 연결 이력</p><div className="flex min-w-max items-center gap-2 text-sm">{nodes.map((node,index)=><span className="contents" key={`${node.type}-${node.id}`}>{index>0&&<span className="text-slate-400">→</span>}<Link className={`rounded-lg border px-3 py-2 ${node.id===currentId?"border-blue-500 bg-blue-50 font-bold text-blue-700":"bg-white hover:border-blue-300"}`} href={node.href as Route}>{node.label} {node.number}</Link></span>)}</div></div>}
