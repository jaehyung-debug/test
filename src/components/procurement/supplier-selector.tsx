"use client";import{useState}from"react";export function SupplierSelector({suppliers}:{suppliers:{id:string;companyName:string;purchaseContactName:string|null;purchaseContactEmail:string|null}[]}){const[selected,setSelected]=useState<string[]>([]);return <div><input type="hidden" name="supplierIds" value={JSON.stringify(selected)}/><div className="grid grid-cols-3 gap-2">{suppliers.map(s=><label className="rounded border p-3 text-sm" key={s.id}><input className="mr-2" type="checkbox" onChange={e=>setSelected(v=>e.target.checked?[...v,s.id]:v.filter(x=>x!==s.id))}/><b>{s.companyName}</b><span className="block text-xs text-slate-500">{s.purchaseContactName??"담당자 미등록"} · {s.purchaseContactEmail??"이메일 미등록"}</span></label>)}</div></div>}
