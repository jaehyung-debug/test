import type{ReactNode}from"react";export function DocumentPartyGrid({leftTitle="공급받는자",rightTitle="공급자",leftRows,rightRows,rightOverlay}:{leftTitle?:string;rightTitle?:string;leftRows:Array<[string,string]>;rightRows:Array<[string,string]>;rightOverlay?:ReactNode}){return <section className="document-parties grid grid-cols-2 border-l border-t border-black"><Party title={leftTitle} rows={leftRows}/><div className="relative border-b border-r border-black"><Party title={rightTitle} rows={rightRows} nested/>{rightOverlay}</div></section>}
function Party({title,rows,nested=false}:{title:string;rows:Array<[string,string]>;nested?:boolean}){return <div className={nested?"":"border-b border-r border-black"}><h2 className="border-b border-black p-2 text-center text-sm font-bold">{title}</h2>{rows.map(([key,value])=><div className="grid grid-cols-[88px_1fr] border-b border-black last:border-b-0" key={key}><b className="border-r border-black p-2">{key}</b><span className="min-h-7 break-words p-2">{value}</span></div>)}</div>}
