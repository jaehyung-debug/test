import { db } from "@/lib/db";import { QuotationForm } from "@/components/forms/quotation-form";import { Notice,PageHeader } from "@/components/common/page";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;const [clients,products]=await Promise.all([db.client.findMany({where:{deletedAt:null},select:{id:true,companyName:true}}),db.product.findMany({where:{deletedAt:null,isActive:true}})]);return <div className="p-8"><PageHeader title="견적서 신규 작성"/><Notice error={p.error}/><QuotationForm clients={clients} products={products.map(x=>({...x,defaultPrice:Number(x.defaultPrice)}))}/></div>}
