import { notFound } from "next/navigation";import { db } from "@/lib/db";import { ProjectForm } from "@/components/forms/project-form";import { Notice,PageHeader } from "@/components/common/page";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {id}=await params,p=await searchParams;const [row,clients,users]=await Promise.all([db.project.findFirst({where:{id,deletedAt:null}}),db.client.findMany({where:{deletedAt:null},select:{id:true,companyName:true}}),db.user.findMany({where:{deletedAt:null,isActive:true},select:{id:true,name:true}})]);if(!row)notFound();return <div className="p-8"><PageHeader title="프로젝트 수정" description={row.projectCode}/><Notice error={p.error}/><ProjectForm clients={clients} users={users} project={row}/></div>}
