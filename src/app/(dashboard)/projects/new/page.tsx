import { db } from "@/lib/db";import { ProjectForm } from "@/components/forms/project-form";import { Notice,PageHeader } from "@/components/common/page";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;const [clients,users]=await Promise.all([db.client.findMany({where:{deletedAt:null},select:{id:true,companyName:true}}),db.user.findMany({where:{deletedAt:null,isActive:true},select:{id:true,name:true}})]);return <div className="p-8"><PageHeader title="프로젝트 신규 등록"/><Notice error={p.error}/><ProjectForm clients={clients} users={users}/></div>}
