import { auth } from "@/auth"; import { Notice, PageHeader } from "@/components/common/page"; import { ProjectForm } from "@/components/forms/project-form"; import { db } from "@/lib/db";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams,s=await auth();const [clients,users,me]=await Promise.all([db.client.findMany({where:{deletedAt:null},orderBy:{companyName:"asc"}}),db.user.findMany({where:{deletedAt:null,isActive:true},orderBy:{name:"asc"}}),db.user.findFirstOrThrow({where:{email:s!.user!.email!}})]);return <div className="p-8"><PageHeader title="프로젝트 등록" description="비용을 관리할 프로젝트의 기본 정보를 입력합니다."/><Notice error={q.error}/><ProjectForm clients={clients} users={users} currentUserId={me.id}/></div>}
