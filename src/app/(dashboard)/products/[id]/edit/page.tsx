import { notFound } from "next/navigation";import { db } from "@/lib/db";import { ProductForm } from "@/components/forms/product-form";import { Notice,PageHeader } from "@/components/common/page";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {id}=await params,p=await searchParams;const row=await db.product.findFirst({where:{id,deletedAt:null}});if(!row)notFound();return <div className="p-8"><PageHeader title="품목 수정"/><Notice error={p.error}/><ProductForm product={row}/></div>}
