import { notFound } from "next/navigation";import { db } from "@/lib/db";import { ClientForm } from "@/components/forms/client-form";import { Notice,PageHeader } from "@/components/common/page";import { deleteClient } from "@/features/mvp/actions";import { SubmitButton } from "@/components/forms/submit-button";
export default async function ClientDetail({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{success?:string;error?:string}>}){const {id}=await params,p=await searchParams;const row=await db.client.findFirst({where:{id,deletedAt:null}});if(!row)notFound();return <div className="p-8"><div className="flex justify-between"><PageHeader title="거래처 상세" description={row.clientCode}/><form action={deleteClient}><input type="hidden" name="id" value={id}/><SubmitButton>삭제</SubmitButton></form></div><Notice success={p.success} error={p.error}/><ClientForm client={row}/></div>}
