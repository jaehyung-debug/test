import { db } from "@/lib/db";import { ExpenseForm } from "@/components/forms/expense-form";import { Notice,PageHeader } from "@/components/common/page";export default async function Page({searchParams}:{searchParams:Promise<{projectId?:string;error?:string}>}){const p=await searchParams;const [projects,categories,suppliers]=await Promise.all([db.project.findMany({where:{deletedAt:null},select:{id:true,projectName:true}}),db.expenseCategory.findMany({where:{isActive:true},select:{id:true,name:true},orderBy:{sortOrder:"asc"}}),db.client.findMany({where:{deletedAt:null},select:{id:true,companyName:true}})]);return <div className="p-8"><PageHeader title="프로젝트 비용 등록"/><Notice error={p.error}/><ExpenseForm {...{projects,categories,suppliers}} projectId={p.projectId}/></div>}
