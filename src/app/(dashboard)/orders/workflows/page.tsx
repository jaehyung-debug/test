import Link from"next/link";import{card,Empty,PageHeader}from"@/components/common/page";import{db}from"@/lib/db";export default async function Page(){const rows=await db.orderWorkflowTemplate.findMany({include:{stages:{orderBy:{sortOrder:"asc"}},_count:{select:{orders:true}}},orderBy:{updatedAt:"desc"}});return <div className="p-8"><PageHeader title="오더 흐름 관리" description="오더 생성 시 복사할 진행단계 템플릿을 관리합니다." href="/orders/workflows/new" actionLabel="흐름 등록"/><div className={`${card} mt-5`}>{!rows.length?<Empty/>:<table className="w-full text-sm"><thead className="bg-slate-50"><tr><th className="p-3 text-left">흐름명</th><th>설명</th><th>단계</th><th>사용 오더</th><th>활성</th><th>수정</th></tr></thead><tbody>{rows.map(x=><tr className="border-t" key={x.id}><td className="p-3 font-semibold">{x.name}</td><td>{x.description}</td><td>{x.stages.map(s=>s.stageName).join(" → ")}</td><td className="text-center">{x._count.orders}</td><td className="text-center">{x.isActive?"활성":"비활성"}</td><td className="text-center"><Link className="text-blue-600" href={`/orders/workflows/${x.id}`}>수정</Link></td></tr>)}</tbody></table>}</div></div>}
