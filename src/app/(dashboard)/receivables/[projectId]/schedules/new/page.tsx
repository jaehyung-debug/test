import{notFound}from"next/navigation";import{Notice,PageHeader}from"@/components/common/page";import{ReceivableScheduleForm}from"@/components/forms/receivable-schedule-form";import{db}from"@/lib/db";export default async function Page({params,searchParams}:{params:Promise<{projectId:string}>;searchParams:Promise<{error?:string}>}){const{projectId}=await params,q=await searchParams,[project,last]=await Promise.all([db.project.findFirst({where:{id:projectId,deletedAt:null}}),db.receivableSchedule.aggregate({where:{projectId},_max:{sequence:true}})]);if(!project)notFound();return <div className="p-8"><PageHeader title="입금계획 추가" description={project.projectName}/><Notice error={q.error}/><ReceivableScheduleForm projectId={projectId} nextSequence={(last._max.sequence??0)+1}/></div>}
