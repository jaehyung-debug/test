import { db } from "@/lib/db";import { PaymentForm } from "@/components/forms/payment-form";import { Notice,PageHeader } from "@/components/common/page";export default async function Page({searchParams}:{searchParams:Promise<{projectId?:string;error?:string}>}){const p=await searchParams;const projects=await db.project.findMany({where:{deletedAt:null},select:{id:true,projectName:true}});return <div className="p-8"><PageHeader title="입금 등록"/><Notice error={p.error}/><PaymentForm projects={projects} projectId={p.projectId}/></div>}
