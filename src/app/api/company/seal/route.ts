import { NextRequest,NextResponse } from "next/server";import {auth}from"@/auth";import{readSeal}from"@/lib/storage";
export async function GET(request:NextRequest){if(!await auth())return new NextResponse("인증이 필요합니다.",{status:401});try{const file=request.nextUrl.searchParams.get("file")??"",data=await readSeal(file),extension=file.split(".").pop()?.toLowerCase(),type=extension==="png"?"image/png":extension==="bmp"?"image/bmp":"image/jpeg";return new NextResponse(new Uint8Array(data),{headers:{"Content-Type":type,"Cache-Control":"private, max-age=3600"}})}catch{return new NextResponse("파일을 찾을 수 없습니다.",{status:404})}}
