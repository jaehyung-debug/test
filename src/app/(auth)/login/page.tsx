import { signIn } from "@/auth";
export default function Login(){return <main className="grid min-h-screen place-items-center bg-slate-100"><form action={async(formData)=>{"use server";await signIn("credentials",{email:formData.get("email"),password:formData.get("password"),redirectTo:"/dashboard"})}} className="w-full max-w-sm rounded-2xl bg-white p-8 shadow"><h1 className="text-2xl font-bold">로그인</h1><p className="mt-2 text-sm text-slate-500">업무 관리 시스템에 로그인하세요.</p><label className="mt-6 block text-sm font-medium">이메일<input name="email" type="email" required className="mt-2 w-full rounded-lg border p-3"/></label><label className="mt-4 block text-sm font-medium">비밀번호<input name="password" type="password" minLength={8} required className="mt-2 w-full rounded-lg border p-3"/></label><button className="mt-6 w-full rounded-lg bg-blue-600 p-3 font-semibold text-white">로그인</button></form></main>}
